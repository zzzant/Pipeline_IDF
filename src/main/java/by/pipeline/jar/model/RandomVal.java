package by.pipeline.jar.model;

import java.io.Serializable;
import java.util.Random;

public class RandomVal implements Serializable{

    /*String name = "Random";
    
    public void executer() {
    Thread delay = new Thread();
    delay.run();
    try {
    delay.sleep(1000);
    } catch (InterruptedException e) {
    e.printStackTrace();
    }
    Random rnd = new Random();
    String [] values = {"COMPLETED", "FAILED", "SKIPPED", "IN PROGRESS"};
    int value = rnd.nextInt(values.length);
    System.out.println(this.name+" "+values[value]);
    }*/

  }
